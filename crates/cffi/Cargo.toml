[package]
name = "tailwindcss-oxide-cffi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib", "cdylib", "staticlib"]

[dependencies]
tailwindcss-oxide = { path = "../oxide" }

[build-dependencies]
cbindgen = "0.29.2"

[dev-dependencies]
tempfile = "3.13.0"
